# Pinned to what's actually running in this project's tested environment --
# `pip install -r requirements.txt` covers everything needed to run the
# bootstrap ETL, the box-score/history/play-by-play backfills, the nba_api
# sync (Phase 3), the agent service (Phase 4), the dashboard (Phase 2), and
# the test suite. This repo isn't pip-installed as a package (see
# pyproject.toml's note on why), so install these directly rather than
# `pip install -e .`.

# --- core ---
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
pandas==3.0.3
pydantic-settings==2.14.2
typer==0.27.0
pyarrow==24.0.0

# --- Phase 3 sync (nba_api) ---
nba_api==1.11.4
tenacity==9.1.4

# --- Phase 4 agent service ---
fastapi==0.139.2
uvicorn==0.51.0
sqlglot==30.13.0
anthropic==0.117.1

# --- Phase 2 dashboard ---
streamlit==1.60.0
plotly==6.9.0
altair==6.2.2
requests==2.34.2

# --- dev / test ---
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.22
